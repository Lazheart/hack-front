# Variables para Vite (prefijo VITE_)

# Base del backend de autenticación (por defecto /api/auth si no se define)
VITE_AUTH_BASE=https://tu-backend.com/api/auth

# Base del backend de incidencias (URL completa)
# Nota: se recortan barras finales automáticamente
VITE_INCIDENT_BASE=https://tu-backend.com/api/incidents
